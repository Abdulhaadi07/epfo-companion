# Sample reviewer accounts

The seeded synthetic database includes four reviewer accounts. Each account has its own persisted employment, PF balance, and claim status.

Password for every sample account:

```text
sample-password
```

| Display name | Login ID | Claim situation |
| --- | --- | --- |
| Aarav Mehta | `sample-ready` | Ready to submit |
| Mira Sen | `sample-under_verification` | Under verification |
| Kabir Rao | `sample-action_required` | Action required |
| Tara Iyer | `sample-rejected` | Rejected |

These credentials are synthetic prototype details only. They are not EPFO credentials and do not connect to government systems.

The login page prefills Mira Sen's account by default and also provides a one-click "Try a sample account" action that authenticates through the same server-side login flow.
